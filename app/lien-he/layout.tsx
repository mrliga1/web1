import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'LiÃªn Há»‡ - Greenia Homes',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
