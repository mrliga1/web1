const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = dir + '/' + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if(file.endsWith('.tsx')) results.push(file); } }); return results; } walk('app').forEach(f => { let c = fs.readFileSync(f, 'utf8'); let m = c.replace(/alert\([^)]+\);/g, '// alert removed'); if(c !== m) fs.writeFileSync(f, m, 'utf8'); });
