const fs = require('fs'); const loc = JSON.parse(fs.readFileSync('./src/lib/locationData.json', 'utf8')); const hcm = loc.find(p => p.name === 'Thành ph? H? Chí Minh'); const wards = [...hcm.wards].sort((a,b) => b.length - a.length); let mappedRaw = 'phu?ng an phú dông, h? chí minh'; let matchedWard = null; for(const ward of wards) { const wName = ward.toLowerCase().trim(); const wShort = wName.replace('phu?ng ', ''); const wardRegex = new RegExp('\b' + wShort + '\b', 'i'); if (mappedRaw.includes(wName) || wardRegex.test(mappedRaw)) { matchedWard = ward; break; } } console.log('MATCHED:', matchedWard);
