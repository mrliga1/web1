import { initializeApp } from 'firebase/app'; import { getFirestore, doc, getDoc } from 'firebase/firestore'; import fs from 'fs'; const config = JSON.parse(fs.readFileSync('./firebase-applet-config.json', 'utf8')); const app = initializeApp(config); const db = getFirestore(app, config.firestoreDatabaseId); async function run() { try { const d = await getDoc(doc(db, 'layouts', 'generalConfig')); console.log(d.data()); process.exit(0); } catch(e) { console.error(e); process.exit(1); } } run();
